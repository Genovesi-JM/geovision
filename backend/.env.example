# GeoVision backend environment (example)
# Copy to `backend/.env` for local dev OR set these in Render Environment.

# --- Required for production ---
# IMPORTANT: do NOT commit real values.

# Used to build Google OAuth redirect_uri and links
BACKEND_BASE=https://geovision-backend-db2f.onrender.com
FRONTEND_BASE=https://genovesi-jm.github.io/geovision

# CORS allowlist (comma-separated)
CORS_ORIGINS=https://genovesi-jm.github.io,http://127.0.0.1:8001,http://localhost:8001

# JWT signing key (must be long + random in production)
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Google OAuth (required if you want Google login)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# OpenAI (optional; enables real AI responses instead of demo mode)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_MODEL=gpt-4.1-mini

# --- Database (optional overrides) ---
# By default the app uses SQLite in the working directory.
# For Render Postgres, set DATABASE_URL to the full connection string.
# Examples:
# DATABASE_URL=sqlite:///./geovision.db
# ACCOUNTS_DATABASE_URL=sqlite:///./accounts.db
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

DATABASE_URL=sqlite:///./geovision.db
ACCOUNTS_DATABASE_URL=sqlite:///./accounts.db

# --- Optional ---
# ENV=prod
# ACCESS_TOKEN_EXPIRES_MINUTES=60
