# GeoVision backend environment (example)
# Copy to `backend/.env` for local dev OR set these in DigitalOcean App Platform.

# --- Required for production ---
# IMPORTANT: do NOT commit real values.

# Used to build Google OAuth redirect_uri and links
BACKEND_BASE=https://geovisionops-sqknb.ondigitalocean.app
FRONTEND_BASE=https://genovesi-jm.github.io/geovision

# CORS allowlist (comma-separated)
CORS_ORIGINS=https://genovesi-jm.github.io,https://geovisionops.com,https://www.geovisionops.com,http://127.0.0.1:8001,http://localhost:8001

# JWT signing key (must be long + random in production)
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Google OAuth (required for Google login)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# Microsoft OAuth / Entra ID (required for Microsoft login)
MICROSOFT_CLIENT_ID=YOUR_MICROSOFT_CLIENT_ID
MICROSOFT_CLIENT_SECRET=YOUR_MICROSOFT_CLIENT_SECRET
MICROSOFT_TENANT_ID=common

# OpenAI (enables real AI chatbot responses instead of demo mode)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_MODEL=gpt-4.1-mini

# --- Database ---
# DigitalOcean sets DATABASE_URL automatically from the managed database.
# For local dev, use SQLite:
DATABASE_URL=sqlite:///./geovision.db

# --- Optional SMTP (for sending emails) ---
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=no-reply@geovisionops.com
# SMTP_PASSWORD=your_password
# SMTP_FROM=no-reply@geovisionops.com
# SMTP_USE_TLS=true

# --- Optional ---
# ENV=prod
# ACCESS_TOKEN_EXPIRES_MINUTES=60
