# Production/runtime dependencies are defined once so local tests and the
# container exercise the same framework and provider versions.
-r requirements.deploy.txt

# RAG / Vector Search Dependencies
numpy>=2.5.3
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0  # Optional: for production vector search (or use faiss-gpu)
